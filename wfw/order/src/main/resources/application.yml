server:
  port: 8000

spring:
  application:
    name: ryan-order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/wfw?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true


eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7000/eureka