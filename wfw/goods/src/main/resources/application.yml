server:
  port: 8400

spring:
  application:
    name: ryan-goods
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/wfw_goods?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:7000/eureka
